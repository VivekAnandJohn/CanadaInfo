//
//  AboutListTableViewCellTests.swift
//  CanadaInfoTests
//
//  Created by Vivek John on 07/07/20.
//  Copyright © 2020 developer. All rights reserved.
//

import XCTest
@testable import CanadaInfo

class AboutListTableViewCellTests: XCTestCase {
    
    let titleOne = "TitleOne"
    let descOne = "DescOne"
    let imageOne = "https://imagePath.com/image.png"

    func testConfigure() {
        /*
        guard let cell = actualCell as? CustomTableViewCell else {
             XCTFail("Expected \(CustomTableViewCell.self), but was \(actualCell)")
             return
        }
        //let cell = AboutListTableViewCell.init(style: .default, reuseIdentifier: "AboutListTableViewCell") as AboutListTableViewCell
        var row = Row()
        row.desc = descOne
        row.title = titleOne

        cell?.configure(item: row)
        XCTAssertEqual(cell?.lblTitle.text, titleOne, "Title text should match") */
    }
}
